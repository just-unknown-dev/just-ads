name: just_ads
description: "Mobile ads (Banner, Interstitial, Rewarded, App Open) with GDPR/UMP consent for the just game engine ecosystem."
version: 0.1.0
homepage: https://github.com/just-unknown-dev/just-ads
repository: https://github.com/just-unknown-dev/just-ads
issue_tracker: https://github.com/just-unknown-dev/just-ads/issues

environment:
  sdk: ^3.11.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
